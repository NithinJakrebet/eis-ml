# Dependencies live in requirements.txt; the modules sit at the repo root and are
# imported directly (experiments and tests add the root to sys.path).
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
